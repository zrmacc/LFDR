% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LocalFDR.R
\name{FDRSummary}
\alias{FDRSummary}
\title{FDR Summary Table}
\usage{
FDRSummary(z, fdr, cutoff = NULL)
}
\arguments{
\item{z}{Observations (z-scores).}

\item{fdr}{Output of \code{\link{FDR}}, a list with components \code{lfdr}
and \code{tFDR} (functions).}

\item{cutoff}{Optional lfdr cutoff. If provided, a logical column
\code{discovery} is added: \code{TRUE} where \code{lfdr(z) <= cutoff}.}
}
\value{
A data frame with columns \code{z}, \code{lfdr}, \code{tFDR}, and
  \code{discovery} (if \code{cutoff} is not \code{NULL}).
}
\description{
Builds a data frame of observations with their local and tail FDR values, and
an optional discovery flag at a given lfdr cutoff.
}
