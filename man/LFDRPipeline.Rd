% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LocalFDR.R
\name{LFDRPipeline}
\alias{LFDRPipeline}
\title{End-to-End Local FDR Pipeline}
\usage{
LFDRPipeline(
  z,
  n_bins = NULL,
  degree = 2,
  fit_method = "poly",
  lower_bound = NULL,
  upper_bound = NULL,
  central_prop = NULL,
  null_method = "ML"
)
}
\arguments{
\item{z}{Observations (z-scores).}

\item{n_bins}{Number of bins for density and FDR estimation. Defaults to
\code{ceiling(length(z) / 25)} if \code{NULL}.}

\item{degree}{Polynomial degree for mixture fit.}

\item{fit_method}{Basis for mixture fit: \code{"ns"} (natural splines) or
\code{"poly"} (orthogonal polynomials).}

\item{lower_bound}{Lower limit of null neighborhood. If \code{NULL}, set from
\code{central_prop} quantiles.}

\item{upper_bound}{Upper limit of null neighborhood. If \code{NULL}, set from
\code{central_prop} quantiles.}

\item{central_prop}{Central proportion of z-scores defining the null
neighborhood when bounds are \code{NULL}. Defaults to 0.5 for central
matching and 0.8 for ML.}

\item{null_method}{Method for empirical null: \code{"CM"} (central matching)
or \code{"ML"} (maximum likelihood). Central matching requires the fitted
density and is used automatically in the pipeline.}
}
\value{
A list with components \code{fit} (output of \code{\link{FitEF}}),
  \code{null} (output of \code{\link{ENull}}), and \code{fdr} (output of
  \code{\link{FDR}}).
}
\description{
Runs the full workflow: fit mixture density (\code{\link{FitEF}}), estimate
empirical null (\code{\link{ENull}}), then compute local and tail FDR
(\code{\link{FDR}}). Returns all intermediate results for inspection or
further use.
}
